[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[dev-packages]
coverage = "*"

[packages]
gsutil = "*"
python-dotenv = "0.21.0"

[requires]
python_version = "3.9.15"

[scripts]
coverage = "coverage run -m unittest -v test/test.py "
test =  "python -m unittest -v test/test.py"
report = "coverage report --omit test/*"
